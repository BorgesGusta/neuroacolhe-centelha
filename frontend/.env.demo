# Nura Web (Frontend) - Build de Demonstração (GitHub Pages)
# Usado via `npm run build:demo` (vite build --mode demo)
# Não faz chamadas reais de API: toda a navegação usa os dados
# mockados de src/data/mockData.ts (persistidos em localStorage).

VITE_DEMO_MODE="true"
